"""Dungeons domain schemas (Phase 5.5c.2).

Currently the dungeons route only returns lists — no request body. The schema
module is kept for parity with other domains so future PATCH/POST admin routes
have a clear home.
"""

__all__: list[str] = []
