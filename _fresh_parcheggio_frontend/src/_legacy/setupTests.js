// Jest test bootstrap — adds `@testing-library/jest-dom` matchers
// (e.g. `toBeInTheDocument`, `toHaveAttribute`) and a minimal axios stub
// so component-under-test imports of `../lib/api` don't crash.
import "@testing-library/jest-dom";
